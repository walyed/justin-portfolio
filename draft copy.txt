import React, { useState, useRef, useEffect, useMemo } from 'react';
import { motion, useScroll, useSpring, useTransform, useInView, AnimatePresence } from 'framer-motion';
import { 
  Terminal, Cpu, Activity, Zap, Eye, Share2, ShieldAlert, 
  Crosshair, Award, FileText, Video, Music, Star, Hash, 
  User, Briefcase, Globe, Layers, BookOpen, Mail, ChevronRight,
  MessageSquare, X, Send, Sparkles, Loader, Trophy, Newspaper, Quote, Heart,
  Linkedin, Github, ArrowRight, ArrowDown, Play, Crown
} from 'lucide-react';

// --- GEMINI API INTEGRATION ---

const callGemini = async (prompt, systemInstruction = "") => {
  const apiKey = ""; // Runtime provided key
  try {
    const response = await fetch(
      `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-flash-preview-09-2025:generateContent?key=${apiKey}`,
      {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({
          contents: [{ parts: [{ text: prompt }] }],
          systemInstruction: systemInstruction ? { parts: [{ text: systemInstruction }] } : undefined,
        }),
      }
    );
    if (!response.ok) throw new Error("API Call Failed");
    const data = await response.json();
    return data.candidates?.[0]?.content?.parts?.[0]?.text || "System Error: Data corrupted.";
  } catch (error) {
    console.error(error);
    return "Connection Error: Neural link unstable. Please try again later.";
  }
};

// --- COMPONENT: FLOATING MESH BACKGROUND ---
const FloatingMeshBackground = () => {
  return (
    <div className="absolute inset-0 overflow-hidden rounded-2xl pointer-events-none z-0">
      <motion.div 
        animate={{ 
          x: [0, 50, -30, 0],
          y: [0, -30, 20, 0],
          scale: [1, 1.2, 1.1, 1],
          opacity: [0.05, 0.12, 0.08, 0.05]
        }}
        transition={{ duration: 10, repeat: Infinity, ease: "easeInOut" }}
        className="absolute -top-20 -left-20 w-[120%] h-[120%] bg-violet-600 blur-[80px] mix-blend-multiply"
      />
      <motion.div 
        animate={{ 
          x: [0, -40, 40, 0],
          y: [0, 40, -40, 0],
          scale: [1, 1.1, 1.3, 1],
          opacity: [0.05, 0.1, 0.05, 0.05]
        }}
        transition={{ duration: 15, repeat: Infinity, ease: "easeInOut", delay: 1 }}
        className="absolute bottom-0 right-0 w-[80%] h-[80%] bg-indigo-500 blur-[100px] mix-blend-multiply"
      />
    </div>
  );
};

// --- PART 1: HERO SECTION (NEURAL ON TOP + DULL PICTURES) ---
const ParticleHero = () => {
  const canvasRef = useRef(null);
  const [currentImageIndex, setCurrentImageIndex] = useState(0);

  const images = useMemo(() => [
    "https://images.unsplash.com/photo-1677442136019-21780ecad995?auto=format&fit=crop&q=80&w=1600",
    "https://images.unsplash.com/photo-1581091226825-a6a2a5aee158?auto=format&fit=crop&q=80&w=1600",
    "https://images.unsplash.com/photo-1551288049-bbbda536339a?auto=format&fit=crop&q=80&w=1600",
    "https://images.unsplash.com/photo-1531746790731-6c2079ee3f08?auto=format&fit=crop&q=80&w=1600",
    "https://images.unsplash.com/photo-1516116216624-53e697fedbea?auto=format&fit=crop&q=80&w=1600",
    "https://images.unsplash.com/photo-1507413245164-6160d8298b31?auto=format&fit=crop&q=80&w=1600"
  ], []);

  useEffect(() => {
    const timer = setInterval(() => {
      setCurrentImageIndex((prev) => (prev + 1) % images.length);
    }, 5000); 
    return () => clearInterval(timer);
  }, [images.length]);

  useEffect(() => {
    const canvas = canvasRef.current;
    if (!canvas) return;
    const ctx = canvas.getContext('2d');
    let animationFrameId;
    let particles = [];
    
    // EXTREMELY DENSE neural net config
    const particleCount = 350; 
    const connectionDistance = 100;
    const mouseDistance = 250;
    const gridSpeed = 0.4;
    let gridOffset = 0;
    let autoTiltAngle = 0;

    let mouse = { x: null, y: null };

    const handleResize = () => {
      canvas.width = window.innerWidth;
      canvas.height = window.innerHeight; 
      initParticles();
    };

    const handleMouseMove = (e) => {
        const rect = canvas.getBoundingClientRect();
        mouse.x = e.clientX - rect.left;
        mouse.y = e.clientY - rect.top;
    };

    const handleMouseLeave = () => {
      mouse.x = null;
      mouse.y = null;
    };

    const handleTouchMove = (e) => {
        if(e.touches && e.touches.length > 0) {
            const rect = canvas.getBoundingClientRect();
            mouse.x = e.touches[0].clientX - rect.left;
            mouse.y = e.touches[0].clientY - rect.top;
        }
    };

    class Particle {
      constructor() {
        this.x = Math.random() * canvas.width;
        this.y = Math.random() * (canvas.height * 0.75);
        this.vx = (Math.random() - 0.5) * 0.3;
        this.vy = (Math.random() - 0.5) * 0.3;
        this.size = Math.random() * 1.5 + 0.5;
        this.baseColor = Math.random() > 0.5 ? '139, 92, 246' : '99, 102, 241';
      }

      update() {
        this.x += this.vx;
        this.y += this.vy;
        if (this.x < 0 || this.x > canvas.width) this.vx = -this.vx;
        if (this.y < 0 || this.y > canvas.height * 0.8) this.vy = -this.vy; 

        if (mouse.x != null) {
          let dx = mouse.x - this.x;
          let dy = mouse.y - this.y;
          let distance = Math.sqrt(dx * dx + dy * dy);
          if (distance < mouseDistance) {
             const forceDirectionX = dx / distance;
             const forceDirectionY = dy / distance;
             const force = (mouseDistance - distance) / mouseDistance;
             this.x -= forceDirectionX * force * 4;
             this.y -= forceDirectionY * force * 4;
          }
        }
      }

      draw() {
        ctx.fillStyle = `rgba(${this.baseColor}, 0.7)`; 
        ctx.beginPath();
        ctx.arc(this.x, this.y, this.size, 0, Math.PI * 2);
        ctx.fill();
      }
    }

    function initParticles() {
      particles = [];
      for (let i = 0; i < particleCount; i++) particles.push(new Particle());
    }

    function drawLandscape() {
        const horizonY = canvas.height * 0.65;
        const centerX = canvas.width / 2;
        let tiltX = 0;
        if (mouse.x !== null) {
            tiltX = (mouse.x - centerX) * 0.03; 
        } else {
            autoTiltAngle += 0.008;
            tiltX = Math.sin(autoTiltAngle) * (canvas.width * 0.04);
        }

        const gradient = ctx.createLinearGradient(0, horizonY - 100, 0, horizonY + 200);
        gradient.addColorStop(0, "rgba(0,0,0,0)");
        gradient.addColorStop(0.5, "rgba(139, 92, 246, 0.15)");
        gradient.addColorStop(1, "rgba(0,0,0,0)");
        ctx.fillStyle = gradient;
        ctx.fillRect(0, horizonY - 100, canvas.width, 300);

        ctx.save();
        ctx.beginPath();
        ctx.rect(0, horizonY, canvas.width, canvas.height - horizonY);
        ctx.clip();

        ctx.strokeStyle = "rgba(139, 92, 246, 0.12)";
        ctx.lineWidth = 1;
        const spacing = canvas.width * 1.5 / 20;
        for(let i = -10; i <= 30; i++) {
            const xBase = (i * spacing) - tiltX;
            ctx.beginPath();
            ctx.moveTo(centerX + (xBase - centerX) * 0.1, horizonY);
            ctx.lineTo(xBase + (xBase - centerX) * 4, canvas.height);
            ctx.stroke();
        }

        gridOffset = (gridOffset + gridSpeed) % 40;
        for(let i = 0; i < 20; i++) {
            const z = i * 40 + gridOffset;
            const yPos = horizonY + (Math.pow(z / 800, 2) * (canvas.height - horizonY));
            if (yPos < canvas.height && yPos > horizonY) {
                const opacity = Math.min(1, (yPos - horizonY) / 200);
                ctx.strokeStyle = `rgba(139, 92, 246, ${opacity * 0.2})`;
                ctx.beginPath();
                ctx.moveTo(0, yPos);
                ctx.lineTo(canvas.width, yPos);
                ctx.stroke();
            }
        }
        ctx.restore();
    }

    function animate() {
      // Keep background transparent so images show through
      ctx.clearRect(0, 0, canvas.width, canvas.height);
      
      // Draw Dense Connections
      for (let i = 0; i < particles.length; i++) {
        particles[i].update();
        particles[i].draw();
        for (let j = i + 1; j < particles.length; j++) {
            const dx = particles[i].x - particles[j].x;
            const dy = particles[i].y - particles[j].y;
            const distance = Math.sqrt(dx * dx + dy * dy);
            if (distance < connectionDistance) {
                ctx.beginPath();
                const opacity = 1 - (distance / connectionDistance);
                ctx.strokeStyle = `rgba(139, 92, 246, ${opacity * 0.35})`; 
                ctx.lineWidth = 0.5;
                ctx.moveTo(particles[i].x, particles[i].y);
                ctx.lineTo(particles[j].x, particles[j].y);
                ctx.stroke();
            }
        }
      }
      drawLandscape();
      animationFrameId = requestAnimationFrame(animate);
    }

    window.addEventListener('resize', handleResize);
    canvas.addEventListener('mousemove', handleMouseMove);
    canvas.addEventListener('mouseleave', handleMouseLeave);
    canvas.addEventListener('touchmove', handleTouchMove, { passive: true });
    handleResize();
    animate();

    return () => {
      window.removeEventListener('resize', handleResize);
      canvas.removeEventListener('mousemove', handleMouseMove);
      canvas.removeEventListener('mouseleave', handleMouseLeave);
      canvas.removeEventListener('touchmove', handleTouchMove);
      cancelAnimationFrame(animationFrameId);
    };
  }, [images]);

  return (
    <div className="relative w-full h-[85vh] lg:h-screen overflow-hidden border-b border-white/5 bg-[#000205]">
        {/* Layer 1: Image Slider (Underneath) */}
        <div className="absolute inset-0 z-10 h-full w-full flex items-center justify-center overflow-hidden pointer-events-none">
             <AnimatePresence initial={false}>
                <motion.div
                  key={currentImageIndex}
                  initial={{ x: '-100%', opacity: 0, scale: 0.5 }}
                  animate={{ x: '0%', opacity: 0.6, scale: 0.5 }}
                  exit={{ x: '100%', opacity: 0, scale: 0.5 }}
                  transition={{ 
                    duration: 1.2, 
                    ease: [0.4, 0, 0.2, 1],
                    opacity: { duration: 0.6 }
                  }}
                  className="absolute inset-0 flex items-center justify-center"
                >
                   <div className="relative w-full h-full max-w-6xl max-h-[85vh] flex items-center justify-center p-8">
                      <img 
                        src={images[currentImageIndex]} 
                        alt="Project Slide"
                        // Applied grayscale and lower brightness/contrast to make it "dull"
                        className="w-auto h-auto max-w-full max-h-full object-contain rounded-2xl shadow-[0_0_80px_rgba(0,0,0,0.8)] grayscale brightness-50 contrast-75 border border-white/5"
                      />
                      <div className="absolute inset-0 bg-gradient-to-b from-transparent via-transparent to-black/40 rounded-2xl pointer-events-none" />
                   </div>
                </motion.div>
             </AnimatePresence>
        </div>

        {/* Layer 2: Atmospheric Glow (In between) */}
        <div className="absolute top-[65%] left-0 w-full h-[150px] bg-violet-600/10 blur-[80px] pointer-events-none z-15" />

        {/* Layer 3: Neural Net Canvas (On top) */}
        <canvas ref={canvasRef} className="absolute inset-0 z-20 pointer-events-auto" />
    </div>
  );
};

// --- PART 2: SIDEBAR NAVIGATION ---

const menuItems = [
    { id: 'about', label: '01. About Me' },
    { id: 'press', label: '02. Media & Press' },
    { id: 'endorsements', label: '03. Endorsements' },
    { id: 'awards', label: '04. Awards' },
    { id: 'leadership', label: '05. Leadership' },
    { id: 'projects', label: '06. Projects' },
    { id: 'community', label: '07. Community' },
    { id: 'publications', label: '08. Publications' },
    { id: 'newsletter', label: '09. Newsletter' },
];

const Sidebar = ({ activeSection }) => (
    <aside className="hidden lg:block lg:col-span-3">
        <div className="sticky top-24 space-y-8">
            <div className="font-black text-3xl tracking-tighter text-black mb-8">JA.</div>
            <nav className="space-y-1">
                {menuItems.map((item) => (
                    <a 
                        key={item.id}
                        href={`#${item.id}`} 
                        className={`block px-4 py-3 text-sm font-bold transition-all duration-300 rounded-lg border-l-4 ${
                            activeSection === item.id 
                            ? 'bg-black text-white border-black'
                            : 'text-black hover:bg-black hover:text-white border-transparent'
                        }`}
                    >
                        {item.label}
                    </a>
                ))}
            </nav>
        </div>
    </aside>
);

// --- PART 3: HOLO CARD (WITH RADIOGRAPHY SCANLINE) ---
const HoloCard = ({ project }) => {
  const [hovered, setHovered] = useState(false);
  
  return (
    <div
      onMouseEnter={() => setHovered(true)}
      onMouseLeave={() => setHovered(false)}
      className="relative w-full rounded-xl bg-slate-950 shadow-xl border border-slate-700/50 group overflow-hidden transition-all duration-500 hover:bg-white hover:border-black flex flex-col md:flex-row h-full"
    >
        <div className="relative w-full md:w-5/12 h-64 md:h-auto overflow-hidden">
             <div className="absolute inset-0 pointer-events-none z-20 overflow-hidden opacity-30">
                <div className="w-full h-[2px] bg-cyan-400/80 shadow-[0_0_10px_#22d3ee] absolute top-0 animate-scanline" />
             </div>
             <div className={`w-full h-full bg-cover bg-center transition-all duration-700 ${hovered ? 'scale-105 filter-none' : 'scale-100 grayscale contrast-125 brightness-75'}`}
                style={{ backgroundImage: `url(${project.image})` }} />
        </div>

        <div className="p-8 flex flex-col justify-center flex-1">
            <div className="flex items-center gap-2 mb-2">
                <span className="text-[10px] uppercase tracking-widest text-violet-400 font-mono group-hover:text-black transition-colors">{project.id}</span>
                {project.status === 'Active' && <span className="w-1.5 h-1.5 rounded-full bg-green-500 animate-pulse" />}
            </div>
            <h4 className="text-2xl font-bold text-white group-hover:text-black transition-colors duration-500 mb-2">{project.title}</h4>
            <div className="text-violet-400 text-xs font-bold uppercase mb-4 tracking-wider group-hover:text-violet-700 transition-colors">{project.role}</div>
            <p className="text-slate-400 text-sm mb-6 leading-relaxed group-hover:text-black transition-colors duration-500">{project.description}</p>
            <div className="flex flex-wrap gap-2">
                {project.stack.map((tech, i) => (
                    <span key={i} className="px-2 py-1 bg-violet-900/20 rounded text-xs text-violet-300 border border-violet-500/20 font-mono group-hover:bg-black group-hover:text-white group-hover:border-black transition-all duration-500">{tech}</span>
                ))}
            </div>
        </div>
    </div>
  );
};

// --- PART 4: MAIN APP ---
const App = () => {
  const [activeSection, setActiveSection] = useState('home');

  const projects = [
    { 
      id: "ONC-01", title: "OncoSense", role: "Founder", status: "Active",
      description: "Portable AI ultrasound system for remote cancer screening (Breast, Thyroid, Liver). Addressing healthcare inequality via edge AI.", 
      image: "https://images.unsplash.com/photo-1576091160399-112ba8d25d1d?ixlib=rb-4.0.3&auto=format&fit=crop&w=1470&q=80", 
      stack: ["Python", "Edge AI", "WebGPU"] 
    },
    { 
      id: "HRO-09", title: "HEROChair", role: "Lead Engineer", status: "Active",
      description: "Brain-computer interface (BCI) allowing hands-free wheelchair control using Muse EEG signals and HoloLens spatial mapping.", 
      image: "https://images.unsplash.com/photo-1517077304055-6e89abbf09b0?ixlib=rb-4.0.3&auto=format&fit=crop&w=1469&q=80", 
      stack: ["C++", "ROS", "Muse EEG"] 
    },
    { 
      id: "WPN-09", title: "SafeSight", role: "Lead Developer", status: "Prototype",
      description: "Wearable Raspberry Pi device for the visually impaired. Detects weapons and dangerous objects in real-time.", 
      image: "https://images.unsplash.com/photo-1555949963-ff9fe0c870eb?ixlib=rb-4.0.3&auto=format&fit=crop&w=1470&q=80", 
      stack: ["TensorFlow", "Raspberry Pi", "OpenCV"] 
    },
    { 
      id: "RBT-04", title: "AssistArm", role: "Lead Engineer", status: "Archived",
      description: "Arduino-based robotic arm designed to assist individuals with limited upper body mobility. Voice controlled.", 
      image: "https://images.unsplash.com/photo-1485827404703-89b55fcc595e?ixlib=rb-4.0.3&auto=format&fit=crop&w=1470&q=80", 
      stack: ["Arduino", "C++", "Servo"] 
    }
  ];

  const Section = ({ id, title, chapter, children }) => {
    const ref = useRef(null);
    const isInView = useInView(ref, { margin: "-45% 0px -45% 0px" });
    useEffect(() => { if (isInView) setActiveSection(id); }, [isInView, id]);
    return (
        <section id={id} ref={ref} className="scroll-mt-24 mb-32">
            <div className="mb-8 border-l-4 border-black pl-4">
                <span className="text-slate-400 font-bold tracking-widest uppercase text-sm mb-1 block">{chapter}</span>
                <h2 className="text-4xl font-black text-black mb-2 tracking-tight">{title}</h2>
            </div>
            {children}
        </section>
    );
  };

  return (
    <div className="bg-slate-50 text-slate-800 font-sans">
      <style>{`
        @keyframes scanline {
            0% { top: 0%; opacity: 0; }
            10% { opacity: 1; }
            90% { opacity: 1; }
            100% { top: 100%; opacity: 0; }
        }
        .animate-scanline {
            animation: scanline 3.5s linear infinite;
        }
      `}</style>

      <main className="w-full">
        <section id="home"><ParticleHero /></section>
        <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-12">
            <div className="grid grid-cols-1 lg:grid-cols-12 gap-12">
                <Sidebar activeSection={activeSection} />
                <div className="lg:col-span-9">
                    {/* 01. ABOUT */}
                    <Section id="about" title="About Me" chapter="Chapter 01">
                        <div className="relative bg-slate-950 shadow-xl border border-slate-700/50 p-8 rounded-2xl overflow-hidden group hover:bg-white hover:border-black transition-all duration-500">
                            <FloatingMeshBackground />
                            <div className="relative z-10 grid md:grid-cols-3 gap-8 items-center">
                                <div className="md:col-span-2">
                                    <p className="text-slate-300 text-lg mb-6 group-hover:text-black transition-colors">I am Joel Amaldas. My journey is defined by a drive to develop low-cost technologies that enhance accessibility. I have earned multiple national awards for projects like brain-controlled wheelchairs and AI cancer screeners.</p>
                                    <div className="flex flex-wrap gap-3">
                                        {['Robotics', 'Neuroscience', 'AI & ML'].map(tag => (
                                            <span key={tag} className="px-4 py-2 bg-slate-800 text-slate-300 rounded-full text-xs font-bold group-hover:bg-black group-hover:text-white transition-all">{tag}</span>
                                        ))}
                                    </div>
                                </div>
                                <div className="h-64 rounded-xl overflow-hidden border border-white/10 shadow-2xl">
                                     <img src="https://images.unsplash.com/photo-1531482615713-2afd69097998?auto=format&fit=crop&w=800" className="w-full h-full object-cover" />
                                </div>
                            </div>
                        </div>
                    </Section>

                    {/* 02. MEDIA */}
                    <Section id="press" title="Media & Press" chapter="Chapter 02">
                        <div className="space-y-6">
                            <div className="relative bg-slate-950 border border-violet-500/20 p-8 rounded-2xl group hover:bg-white hover:border-black transition-all duration-500">
                                <FloatingMeshBackground />
                                <div className="relative z-10">
                                    <h3 className="text-2xl font-bold text-white group-hover:text-black mb-2 transition-colors">Top 20 Under 20: Class of 2026</h3>
                                    <p className="text-slate-300 group-hover:text-black transition-colors">Awarded by Avenue Magazine & Calgary YMCA. Recognized for leadership and tech innovation.</p>
                                </div>
                            </div>
                        </div>
                    </Section>

                    {/* 03. ENDORSEMENTS */}
                    <Section id="endorsements" title="Endorsements" chapter="Chapter 03">
                        <div className="grid md:grid-cols-2 gap-6">
                            {[
                                { name: "Dr. David Barzilai", role: "Longevity Medicine", initial: "DB", quote: "Praise for dedication to Precision Health & Longevity." },
                                { name: "Interaxon Inc.", role: "Muse Neurotech", initial: "M", quote: "Official recognition of the 'brilliant students' using Muse technology." },
                                { name: "Lisa Davis", role: "Samsung Canada", initial: "LD", quote: "Announcement of National Winners and recognition of student innovation." },
                                { name: "Darrol Baker", role: "Tech Industry Leader", initial: "DB", quote: "Recommendation regarding AI passion and future potential." }
                            ].map((item, i) => (
                                <div key={i} className="relative bg-slate-950 shadow-lg p-8 rounded-2xl border border-slate-700/50 hover:bg-white hover:border-black transition-all duration-500 group overflow-hidden">
                                    <div className="absolute inset-0 bg-violet-500/5 opacity-0 group-hover:opacity-100 transition-opacity duration-500" />
                                    <Quote className="absolute top-8 right-8 text-white/5 w-8 h-8 group-hover:text-black/10 transition-colors duration-500" />
                                    <p className="text-slate-300 italic mb-6 leading-relaxed relative z-10 group-hover:text-black transition-colors duration-500">"{item.quote}"</p>
                                    <div className="flex items-center gap-3 relative z-10">
                                        <div className="w-10 h-10 bg-slate-800 rounded-full flex items-center justify-center font-bold text-white text-xs border border-white/10 group-hover:bg-black group-hover:text-white group-hover:border-black transition-all duration-500">{item.initial}</div>
                                        <div>
                                            <div className="text-white font-bold text-sm group-hover:text-black transition-colors duration-500">{item.name}</div>
                                            <div className="text-slate-500 text-xs uppercase tracking-wide group-hover:text-violet-600 transition-colors duration-500">{item.role}</div>
                                        </div>
                                    </div>
                                </div>
                            ))}
                        </div>
                    </Section>

                    {/* 04. AWARDS */}
                    <Section id="awards" title="Awards & Honors" chapter="Chapter 04">
                        <div className="grid gap-4">
                            <div className="relative p-6 bg-slate-950 shadow-lg border border-violet-500/20 rounded-xl flex items-center gap-4 overflow-hidden group hover:bg-white hover:border-black transition-all duration-500">
                                <FloatingMeshBackground />
                                <Trophy className="w-8 h-8 text-violet-500 relative z-10" />
                                <div className="relative z-10">
                                    <h3 className="text-xl font-bold text-white group-hover:text-black transition-colors duration-500">Samsung Solve for Tomorrow</h3>
                                    <p className="text-violet-400 text-sm font-bold group-hover:text-violet-700">National 2nd Place ($10k) + Fan Favourite ($5k)</p>
                                </div>
                            </div>
                            <div className="grid md:grid-cols-2 gap-4">
                                <div className="p-6 bg-slate-950 shadow-md border border-slate-700/50 rounded-xl hover:bg-white hover:border-black transition-all duration-500 group relative overflow-hidden">
                                    <h3 className="font-bold text-white mb-1 relative z-10 group-hover:text-black transition-colors duration-500">CWSF National</h3>
                                    <p className="text-sm text-slate-400 relative z-10 group-hover:text-slate-700">2x Bronze Medalist (2024, 2025)</p>
                                </div>
                                <div className="p-6 bg-slate-950 shadow-md border border-slate-700/50 rounded-xl hover:bg-white hover:border-black transition-all duration-500 group relative overflow-hidden">
                                    <h3 className="font-bold text-white mb-1 relative z-10 group-hover:text-black transition-colors duration-500">CYSF Regional</h3>
                                    <p className="text-sm text-slate-400 relative z-10 group-hover:text-slate-700">4x Gold Medalist (2021, 2023, 2024, 2025)</p>
                                </div>
                            </div>
                        </div>
                    </Section>

                    {/* 05. LEADERSHIP */}
                    <Section id="leadership" title="Leadership" chapter="Chapter 05">
                        <div className="grid md:grid-cols-2 gap-6">
                            {[
                                { title: "Calgary Youth Mayor Council", date: "2025-2026", role: "Council Member", sub: "CITY OF CALGARY", icon: Crown },
                                { title: "Young Canadians' Parliament", date: "2025-2026", role: "Parliament Member", sub: "FEDERAL INITIATIVE", icon: Globe },
                                { title: "Royal Canadian Air Cadets", date: "Active", role: "Air Cadet", sub: "LEADERSHIP & FLIGHT", icon: Award },
                                { title: "CAYAC", date: "Advisor", role: "Board Advisor", sub: "HEALTHCARE ADVOCACY", icon: Heart }
                            ].map((item, i) => (
                                <div key={i} className="relative bg-slate-950 shadow-lg p-6 rounded-xl border border-slate-700/50 hover:bg-white hover:border-black transition-all duration-500 group overflow-hidden">
                                    <div className="relative z-10 flex items-center justify-between mb-2">
                                        <h4 className="font-bold text-white text-lg group-hover:text-black transition-colors duration-500">{item.title}</h4>
                                        <span className="text-xs font-mono text-violet-400 bg-violet-400/10 px-2 py-1 rounded group-hover:bg-black group-hover:text-white transition-all duration-500">{item.date}</span>
                                    </div>
                                    <p className="text-slate-400 text-sm mb-3 relative z-10 group-hover:text-black transition-colors duration-500">{item.role}</p>
                                    <div className="text-xs text-slate-500 font-mono flex items-center gap-1 relative z-10 group-hover:text-slate-600"><item.icon className="w-3 h-3" /> {item.sub}</div>
                                </div>
                            ))}
                        </div>
                    </Section>

                    {/* 06. PROJECTS */}
                    <Section id="projects" title="Projects" chapter="Chapter 06">
                        <div className="space-y-8">
                            {projects.map((p) => <HoloCard key={p.id} project={p} />)}
                        </div>
                    </Section>

                    {/* 07. COMMUNITY */}
                    <Section id="community" title="Lifting the Community" chapter="Chapter 07">
                        <div className="relative bg-slate-950 border border-violet-500/20 shadow-xl rounded-3xl p-8 md:p-12 text-center overflow-hidden group hover:bg-white hover:border-black transition-all duration-500">
                            <FloatingMeshBackground />
                            <div className="relative z-10">
                                <h2 className="text-3xl font-bold text-white mb-6 group-hover:text-black transition-colors duration-500">Silver AI Initiative</h2>
                                <p className="text-slate-300 mb-8 max-w-xl mx-auto text-lg leading-relaxed group-hover:text-black transition-colors duration-500">Bridging the generational gap. We conduct seminars, provide books, and training to educate seniors in the AI era.</p>
                                <div className="flex justify-center">
                                    <a href="#" className="px-8 py-3 rounded-lg bg-violet-600 text-white font-bold hover:bg-violet-500 transition-all flex items-center justify-center gap-2">
                                        Donate via GoFundMe <ChevronRight className="w-4 h-4" />
                                    </a>
                                </div>
                            </div>
                        </div>
                    </Section>

                    {/* 08. PUBLICATIONS */}
                    <Section id="publications" title="Publications" chapter="Chapter 08">
                        <div className="space-y-4">
                            <a href="https://medium.com/@joel.amaldas/scintix-how-ai-driven-radiotherapy-from-reflexion-is-revolutionizing-cancer-treatment-at-stanford-2575b5e43755" target="_blank" className="relative block p-6 rounded-xl bg-slate-950 shadow-lg border border-slate-700/50 hover:bg-white hover:border-black transition-all duration-500 group overflow-hidden">
                                <div className="relative z-10">
                                    <div className="text-violet-400 text-xs font-bold mb-1">MEDIUM</div>
                                    <h3 className="text-xl font-bold text-white mb-2 group-hover:text-black transition-colors duration-500">Scintix: AI-Driven Radiotherapy at Stanford</h3>
                                    <p className="text-sm text-slate-400 group-hover:text-slate-700 transition-colors duration-500">Technical analysis of RefleXion's biology-guided radiotherapy.</p>
                                </div>
                            </a>
                        </div>
                    </Section>

                    {/* 09. NEWSLETTER */}
                    <Section id="newsletter" title="Newsletter" chapter="Chapter 09">
                        <div className="grid md:grid-cols-3 gap-8">
                            <div className="md:col-span-2 bg-slate-950 p-8 rounded-2xl group hover:bg-white hover:border-black transition-all duration-500 border border-transparent">
                                <h3 className="text-2xl font-bold text-white group-hover:text-black mb-4">Join the Conversation</h3>
                                <input type="email" placeholder="Email address" className="w-full bg-black/50 border border-white/10 rounded-lg px-4 py-3 text-white focus:outline-none group-hover:bg-slate-100 group-hover:text-black group-hover:border-black transition-all" />
                            </div>
                            <div className="space-y-4">
                                <h4 className="text-sm font-bold text-black uppercase">Past Issues</h4>
                                <a href="#" className="block p-4 rounded-lg bg-slate-950 hover:bg-white hover:border-black border border-white/5 transition-all duration-500 group">
                                    <div className="text-sm text-white group-hover:text-black font-bold">The Ethics of BCI Data</div>
                                </a>
                                <a href="#" className="block p-4 rounded-lg bg-slate-950 hover:bg-white hover:border-black border border-white/5 transition-all duration-500 group">
                                    <div className="text-sm text-white group-hover:text-black font-bold">Generative AI in School</div>
                                </a>
                            </div>
                        </div>
                    </Section>

                    <footer className="pt-24 pb-12 text-black text-xs font-mono border-t border-black/10 mt-12">&copy; 2026 Joel Amaldas. Built to Innovate.</footer>
                </div>
            </div>
        </div>
      </main>
    </div>
  );
};

export default App;